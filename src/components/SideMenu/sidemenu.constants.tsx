export const SIDE_MENU_PX = 6 as const

/** These styles are needed so hover has the effect of highlighting the whole tile whilst still keeping the text aligned to the date header */
export const SIDE_MENU_ITEM_PX = 3 as const
export const SIDE_MENU_CONVERSATION_TITLE_PX = SIDE_MENU_PX - SIDE_MENU_ITEM_PX
